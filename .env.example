# Frontend (Vite) — expostas ao navegador, nunca coloque segredos aqui.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Modo de demonstração local sem backend real (ver README).
# NUNCA definir em produção.
VITE_MOCK_AUTH=false

# Edge Functions (Supabase Secrets) — nunca no bundle da Vercel.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
CATALOG_READ_SECRET=
EXTERNAL_ORDERS_SECRET=
INTEGRATION_CALLBACK_SECRET=
